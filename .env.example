# Copy to .env and fill in. .env is gitignored; this file is not.
#
# Create a key at https://my.vultr.com/settings/#settingsapi. Keys are
# IP-restricted by default, so add the machine that runs the controller to the
# allowed list there as well, or every call comes back 403.
VULTR_API_KEY=
